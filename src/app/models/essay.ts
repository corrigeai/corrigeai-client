export class Essay {
    title: string;
    theme: string;
    textBody: string;

    constructor(title: string, theme: string, textBody: string) {
        this.title = title;
        this.theme = theme;
        this.textBody = textBody;
    }
}